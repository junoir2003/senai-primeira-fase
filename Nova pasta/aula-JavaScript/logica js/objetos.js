const usuario = {
     login: "admin",
     email:"jr@gmail.com",
     saldo:50000,
     isLogin:true,
    senha:"123456"
}

console.log(`o email do usuario é ${usuario.email} e o saldo é ${usuario.saldo}`)